services:
  - type: web
    name: ev-charge-project
    runtime: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt && python create_db.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11